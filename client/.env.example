# Base URL of the backend (scheme + host, no trailing slash).
# Leave unset in development — Vite proxies /api → http://localhost:3001.
# For a split deployment set e.g. VITE_API_BASE_URL=https://your-api.up.railway.app
# VITE_API_BASE_URL=
